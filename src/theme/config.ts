/*
  To make the dark or light mode persist in the form of local storage
  And also to set a initial color mode
*/

const config = {
  initialColorMode: "dark",
  useSystemColorMode: false,
};

export default config;
