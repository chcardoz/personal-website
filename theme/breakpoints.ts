import { createBreakpoints } from "@chakra-ui/theme-tools";

const breakpoints = createBreakpoints({
  sm: "320px", //mobile S
  md: "768px", //tablet
  lg: "960px", //laptop
  xl: "1200px", //laptop L or maybe 4K
});

export default breakpoints;
